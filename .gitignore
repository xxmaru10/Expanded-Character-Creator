# Build artifacts (regenerated by `dotnet build`) — not the installer content.
src/bin/
src/obj/

# Third-party BepInEx binaries needed only to COMPILE/PACKAGE the mod (not our code).
# See DESIGN.md / README.txt for where to download them before building.
lib/
third_party/
